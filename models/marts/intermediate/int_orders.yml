models:
  - name: int_orders
    description: intermetiate model joining customers and location data.
    access: private
    group: finance
    columns:
      - name: order_id
        tests:
          - unique